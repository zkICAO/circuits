[package]
name = "nullifier"
type = "lib"
authors = []
compiler_version = ">=1.0.0"

[dependencies]
commit = { path = "../../core/commit" }
policy = { path = "../../core/policy" }
predicate = { path = "../../claims/predicate" }
