# Generated by fixtures/generator with the templates subcommand. Do not edit.
#
# Copy this to Prover.toml and replace every placeholder with your own
# material. Nothing here is a working value: a witness holds one holder's
# document, so no real one is committed anywhere in this repository.
#
# Where a value comes from is written beside it. The witness tools under
# tools/ derive the ones that are hashes of something else, so that they are
# computed by the same library the circuit checks them against.

# from the verification key of that inner circuit, as 115 field elements: bb write_vk
sod_key = ["<your sod key>", "<your sod key>", "..."]  # 115 elements in total

# from that inner circuit's proof, as 500 field elements: bb prove
sod_proof = ["<your sod proof>", "<your sod proof>", "..."]  # 500 elements in total

# from the verification key of that inner circuit, as 115 field elements: bb write_vk
dg_extract_key = ["<your dg extract key>", "<your dg extract key>", "..."]  # 115 elements in total

# from that inner circuit's proof, as 500 field elements: bb prove
dg_extract_proof = ["<your dg extract proof>", "<your dg extract proof>", "..."]  # 500 elements in total

# from the verification key of that inner circuit, as 115 field elements: bb write_vk
attributes_key = ["<your attributes key>", "<your attributes key>", "..."]  # 115 elements in total

# from that inner circuit's proof, as 500 field elements: bb prove
attributes_proof = ["<your attributes proof>", "<your attributes proof>", "..."]  # 500 elements in total

# from the verification key of that inner circuit, as 115 field elements: bb write_vk
anchor_key = ["<your anchor key>", "<your anchor key>", "..."]  # 115 elements in total

# from that inner circuit's proof, as 500 field elements: bb prove
anchor_proof = ["<your anchor proof>", "<your anchor proof>", "..."]  # 500 elements in total

# from return[0] of the sod proof
econtent_binding = "<your econtent binding>"

# from return[0] of the attributes proof
dsc_commitment = "<your dsc commitment>"

# from return[2] of the sod proof
secret_binding = "<your secret binding>"

# from return[0] of the dg_extract proof
dg_binding = "<your dg binding>"

# from return[0] of the attributes proof
commitment = "<your commitment>"

# the date this proof is made, as YYYYMMDD
current_yyyymmdd = "<your current yyyymmdd>"

# from tools/merkle_path over tools/modulus_leaf entries
master_list_root = "<your master list root>"

# your application, a non zero value distinct from every other application
domain = "<your domain>"

# a fresh non zero value the verifier issued for this exchange
context = "<your context>"
